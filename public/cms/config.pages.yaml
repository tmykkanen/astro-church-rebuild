collections:
  - name: pages
    label: Pages
    label_singular: Page
    folder: src/content/pages
    media_folder: "{{media_folder}}/pages"
    fields:
      - name: title
        label: Title
        widget: string
      - name: body
        label: Body
        widget: markdown
